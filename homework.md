# 4/13宿題
- 実空間でポアソン方程式を解く。
- 誤差がグリッドを細かくすると、解析解に近づくことを確かめる。

# 4/23宿題
- 短距離・長距離を分ける

# 4/27
- 距離ベースのクーロンポテンシャルの問題をci*exp(-αr^2)を基底として展開する
- ポテンシャルエネルギーの誤差の目標値: E=ρ(^T)(Kρ)
これがO(10^-3)が目標

# 5/1
実装を直す
時間を測る
- %%timeit
- %%time
評価基準をエネルギーにする
rPCAを最後までやって、LとSのトレードオフについても確認する
指数関数近似に対し、次元削減手法を試みる

# 5/2
特異値分解による計算量削減の効果を確認する
- SVDの目標は計算量を削減すること
- %%time を使って、普通に計算した場合と SVD で低ランク近似した場合の実行時間の差を出す
- 既存の手法では行列積の評価順によっては計算量の差が出ないため、かける順番に注意する
  - 例: U Σ V^T x を (U (Σ (V^T x))) の順で評価することで O(mn) を O(r(m+n)) に落とす
  - フルランクのまま (U Σ V^T) を先に組み立てて x にかけると低ランクの恩恵が消える

# 5/11
Nを増やしてみる
ロバスト多めにRPCAを変える